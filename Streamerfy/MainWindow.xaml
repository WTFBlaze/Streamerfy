<Window x:Class="Streamerfy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Streamerfy"
        Height="500" Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="False"
        Background="#1e1e1e">
    
    <Window.Resources>
        <!-- Close Button Style -->
        <Style x:Key="RoundedCloseButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="Transparent"
                                CornerRadius="0"
                                Padding="0"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#e81123"/>
                                <Setter TargetName="border" Property="CornerRadius" Value="0,10,0,0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#c50f1f"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="40"/>
        </Style>
        
        <!-- Tab Button Style -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Hover effect -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2a2a2a"/>
                            </Trigger>
                            <!-- Selected tab effect -->
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="selected">
                                <Setter TargetName="border" Property="Background" Value="#3a3a3a"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Toggle Switch Style -->
        <Style x:Key="ToggleSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <!-- Track -->
                            <Border x:Name="SwitchBorder"
                            Background="#3a3a3a"
                            CornerRadius="12"
                            Height="24"
                            Width="50"/>

                            <!-- Knob -->
                            <Ellipse x:Name="SwitchKnob"
                             Fill="White"
                             Width="20"
                             Height="20"
                             Margin="2"
                             HorizontalAlignment="Left"
                             VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchBorder" Property="Background" Value="#007acc"/>
                                <Setter TargetName="SwitchKnob" Property="HorizontalAlignment" Value="Right"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="SwitchBorder" Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Section Toggle Button Style -->
        <Style x:Key="SectionToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="#2d2d30"/>
            <!-- Background for header -->
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}"
                        CornerRadius="4"
                        Padding="{TemplateBinding Padding}">
                            <Grid>
                                <TextBlock Text="{TemplateBinding Content}"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"/>

                                <TextBlock Text="▶"
                                   x:Name="Arrow"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Center"
                                   Margin="0,0,10,0"
                                   RenderTransformOrigin="0.5,0.5">
                                    <TextBlock.RenderTransform>
                                        <RotateTransform Angle="0"/>
                                    </TextBlock.RenderTransform>
                                </TextBlock>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Arrow" Property="Text" Value="▼"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#383838"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="#1e1e1e" CornerRadius="10" BorderBrush="#444" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/> <!-- Header -->
                <RowDefinition Height="40"/> <!-- Tabs -->
                <RowDefinition Height="*"/> <!-- Content -->
                <RowDefinition Height="40"/> <!-- Status Bar (new) -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="#2d2d30"
                    CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Streamerfy"
                               FontSize="16"
                               Foreground="White"
                               VerticalAlignment="Center"
                               Margin="10,0"/>
                    <Button Style="{StaticResource RoundedCloseButtonStyle}"
                            Content="✕"
                            Click="Close_Click"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0"/>
                </Grid>
            </Border>
            
            <!-- Tabs -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Background="#1f1f1f" Height="40">
                <Button x:Name="TabLogs"
                    Content="Logs"
                    Style="{StaticResource TabButtonStyle}"
                    Click="Tab_Logs_Click"/>
                
                <Button x:Name="TabSettings"
                    Content="Settings"
                    Style="{StaticResource TabButtonStyle}"
                    Click="Tab_Settings_Click"/>
            </StackPanel>

            <!-- Logs Panel -->
            <Grid x:Name="LogsPanel"
                Grid.Row="2"
                Visibility="Visible"
                Margin="10"
                Background="#252526">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Logs Page"
                    Foreground="White"
                    FontSize="16"
                    FontWeight="Bold"
                    Margin="10"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"/>

                <!-- RichTextBox stays scrollable here -->
                <RichTextBox x:Name="LogBox"
                    Grid.Row="1"
                    IsReadOnly="True"
                    VerticalScrollBarVisibility="Auto"
                    Background="#1e1e1e"
                    BorderThickness="0"
                    FontFamily="Cascadia Code"
                    FontSize="13"
                    Foreground="White"
                    Padding="5"
                    Margin="10"/>
            </Grid>

            <!-- Settings Panel -->
            <ScrollViewer x:Name="SettingsPanel"
                Grid.Row="2"
                Visibility="Collapsed"
                Margin="10"
                Background="#252526"
                VerticalScrollBarVisibility="Auto"
                IsHitTestVisible="True"
                Focusable="True">

                <StackPanel Margin="10" Orientation="Vertical" VerticalAlignment="Top" >
                    <!-- Title -->
                    <TextBlock Text="Settings Page"
                        Foreground="White"
                        FontSize="16"
                        FontWeight="Bold"
                        Margin="0,0,0,20"/>

                    <StackPanel Margin="0,0,0,10" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Auto-connect on launch"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0"
                            Foreground="White"
                            FontSize="13"/>

                        <ToggleButton x:Name="AutoConnectToggle"
                            Style="{StaticResource ToggleSwitchStyle}"
                            IsChecked="False"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Block Explicit Songs"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0"
                            Foreground="White"
                            FontSize="13"/>

                        <ToggleButton x:Name="BlockExplicitToggle"
                            Style="{StaticResource ToggleSwitchStyle}"
                            IsChecked="False"/>
                    </StackPanel>

                    <!-- Twitch Section -->
                    <ToggleButton x:Name="TwitchSectionToggle"
                        Content="Twitch Bot Settings"
                        Style="{StaticResource SectionToggleButtonStyle}"
                        IsChecked="False"
                        Checked="TwitchSectionToggle_Checked"
                        Unchecked="TwitchSectionToggle_Unchecked"
                        Margin="0,0,0,10"/>

                    <StackPanel x:Name="TwitchSettingsPanel"
                        Visibility="Collapsed"
                        Margin="10,0,0,10">

                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Bot Username" Foreground="White" FontSize="13"/>
                            <TextBox x:Name="UsernameField"
                                Background="#1e1e1e"
                                Foreground="White"
                                BorderBrush="#444"
                                BorderThickness="1"
                                Padding="5"
                                FontSize="13"
                                TextChanged="UsernameField_TextChanged"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Bot OAuth" Foreground="White" FontSize="13"/>
                            <TextBox x:Name="OAuthField"
                                Background="#1e1e1e"
                                Foreground="White"
                                BorderBrush="#444"
                                BorderThickness="1"
                                Padding="5"
                                FontSize="13"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Channel" Foreground="White" FontSize="13"/>
                            <TextBox x:Name="ChannelField"
                                Background="#1e1e1e"
                                Foreground="White"
                                BorderBrush="#444"
                                BorderThickness="1"
                                Padding="5"
                                FontSize="13"/>
                        </StackPanel>

                    </StackPanel>

                    <!-- Spotify Section -->
                    <ToggleButton x:Name="SpotifySectionToggle"
                        Content="Spotify Client Settings"
                        Style="{StaticResource SectionToggleButtonStyle}"
                        IsChecked="False"
                        Checked="SpotifySectionToggle_Checked"
                        Unchecked="SpotifySectionToggle_Unchecked"
                        Margin="0,20,0,10"/>

                    <StackPanel x:Name="SpotifySettingsPanel"
                        Visibility="Collapsed"
                        Margin="10,0,0,10">

                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Spotify Client ID" Foreground="White" FontSize="13"/>
                            <TextBox x:Name="ClientIDField"
                                Background="#1e1e1e"
                                Foreground="White"
                                BorderBrush="#444"
                                BorderThickness="1"
                                Padding="5"
                                FontSize="13"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="Spotify Client Secret" Foreground="White" FontSize="13"/>
                            <TextBox x:Name="ClientSecretField"
                                Background="#1e1e1e"
                                Foreground="White"
                                BorderBrush="#444"
                                BorderThickness="1"
                                Padding="5"
                                FontSize="13"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Save Button -->
                    <Button Content="Save Settings"
                        Click="SaveSettings_Click"
                        Background="#3a3a3a"
                        Foreground="White"
                        Padding="8,4"
                        FontSize="13"
                        BorderThickness="0"
                        Cursor="Hand"
                        HorizontalAlignment="Left"
                        Width="150"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Status Bar -->
            <Grid Grid.Row="3" Background="#1e1e1e">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Bottom-left: connection status -->
                <TextBlock x:Name="ConnectionStatusText"
                    Text="● Not Connected"
                    FontSize="12"
                    Foreground="Red"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Margin="10,5,0,5"
                    Grid.Column="0"/>

                <!-- Bottom-right: Connect button -->
                <Button x:Name="ConnectButton"
                    Content="Connect"
                    Click="ConnectButton_Click"
                    Background="#3a3a3a"
                    Foreground="White"
                    FontSize="12"
                    Padding="8,4"
                    Margin="0,5,10,5"
                    BorderThickness="0"
                    Cursor="Hand"
                    HorizontalAlignment="Right"
                    Grid.Column="1"/>
            </Grid>
        </Grid>
    </Border>
</Window>
